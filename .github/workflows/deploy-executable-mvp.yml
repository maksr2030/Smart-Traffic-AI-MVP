name: Deploy Executable Smart Traffic MVP from Feature Branch

on:
  push:
    branches: [main]
    paths: ['.github/workflows/deploy-executable-mvp.yml']
  workflow_run:
    workflows: ["Smart Traffic MVP CI"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-production
  cancel-in-progress: true

jobs:
  deploy-feature-mvp:
    if: >-
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.head_branch == 'feature/unified-traffic-mvp-v1')
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout executable MVP feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/unified-traffic-mvp-v1
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate registry and evidence
        run: npm run validate
      - name: Run automated tests
        run: npm test
      - name: Check executable wiring
        run: npm run check
      - name: Build the same v1.9 executable artifact used by E2E
        run: |
          node scripts/build-executable-site-v190.mjs site
          node --check site/app.js
          node --check site/v18Runtime.js
          node --check site/acquisitionRuntime.js
          node --check site/guidedDemoRuntime.js
          node --check site/decisionRoomRuntime.js
          node --check site/hardeningRuntime.js
          node --check site/engine/auditHash.js
          node --check site/engine/unifiedStateBus.js
          node --check site/engine/decisionLedgerEngine.js
          node --check site/engine/exactReplayEngine.js
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
